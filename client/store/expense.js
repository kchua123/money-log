import axios from "axios";

// Action Type



// Action Creator


// Thunks


// Sub-Reducer
const initialState = [];

export default (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};